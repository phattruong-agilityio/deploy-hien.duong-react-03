export enum COLORS {
  DEFAULT = 'default',
  PRIMARY = 'primary',
  SUCCESS = 'success',
  WARNING = 'waring',
  DANGER = 'danger'
}

export enum SIZES {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large'
}

export enum VARIANTS {
  TEXT = 'text',
  CONTAINED = 'contained',
  OUTLINED = 'outlined'
}

export enum BUTTON_VARIANTS {
  TEXT = VARIANTS.TEXT,
  CONTAINED = VARIANTS.CONTAINED,
  OUTLINED = VARIANTS.OUTLINED
}

export enum BUTTON_COLORS {
  DEFAULT = COLORS.DEFAULT,
  PRIMARY = COLORS.PRIMARY,
  DANGER = COLORS.DANGER
}

export enum BUTTON_SIZES {
  SMALL = SIZES.SMALL,
  MEDIUM = SIZES.MEDIUM,
  LARGE = SIZES.LARGE
}
